## Edge

<!-- Environment and organization. -->

## Confirmations

- [ ] The role (`operates` / `built_for` / `licenses` / `hosts`) is stated exactly as the source supports it — no inference from one role to another.
- [ ] Every role's source is a `PLATFORM_STATEMENT` or `THIRD_PARTY_REPORTING` — not an announcement from either named party about itself or the other.
- [ ] `as_of` reflects the source's actual publish or retrieval date.
- [ ] This edge does not duplicate an existing one for the same environment-organization pair (add a role to the existing file instead).
- [ ] The entry contains only the template's fields — no deal terms, history, notes, or evaluative language.
- [ ] `INDEX.md` updated with an alphabetical-by-environment row.
